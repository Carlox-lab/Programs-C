#include <stdio.h>
#include <stdlib.h>

int main (void)
{
    int n;

    printf("Introduzca tamaño de tablero (nxn):");
    scanf("%d" ,&n);

    if (n < 2)
    {
        return 0;
    }
    else
    {
        crea_tablero(n);
    }

    return 0;
}

int crea_tablero(n);
{
    int coord[];

    coord=poner_bombas;
}

int poner_bombas(n);
{
    
}